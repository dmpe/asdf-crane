name: Build

on:
  push:
    branches:
      - master
      - main
  pull_request:

jobs:
  plugin_test_containers:
    name: asdf plugin test (on containers)
    runs-on: ubuntu-latest
    container:
      image: ${{ matrix.container }}
    strategy:
      matrix:
        container:
          - ubuntu:latest
          # - fedora:rawhide
    steps:
      - name: install commands such as github.ref_name
        run: apt update && install git -y
      - name: asdf_plugin_test
        uses: asdf-vm/actions/plugin-test@master
        with:
          command: crane version
          asdf_branch: ${{ github.ref_name }}

  plugin_test_os:
    name: asdf plugin test (on OS)
    strategy:
      matrix:
        os:
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
      - name: asdf_plugin_test
        uses: asdf-vm/actions/plugin-test@master
        with:
          command: crane version
          asdf_branch: 1-improvements
